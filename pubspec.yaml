name: lsp
description: Dart binding for the Language Server Protocol.
version: 0.0.0
author: Tobe O <thosakwe@gmail.com>
environment:
  sdk: ">=1.19.0"
dependencies:
  owl: ^0.2.0
dev_dependencies:
  build_runner: ^0.3.0
  owl_codegen: ^0.2.0
  test: ^0.12.0